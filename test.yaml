default:
  target: dev
  outputs:
    dev:
      type: redshift
      host: default-workgroup.664418951438.ap-south-1.redshift-serverless.amazonaws.com  # Example: my-redshift-cluster.us-east-1.redshift.amazonaws.com
      port: 5439
      user: admin
      password: Radha31045
      dbname: h24ajinkya
      schema: devstage          # Example: devstage
      threads: 4                             # Number of parallel threads
      keepalives: 1
      keepalives_idle: 300
